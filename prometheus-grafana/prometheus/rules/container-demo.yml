groups:
- name: Container
  rules:
  - alert: Container Tắt
    expr: time() - container_last_seen > 60
    for: 0s
    labels:
      severity: critical
    annotations:
      summary: Container killed (instance {{ $labels.instance }})
      description: |
       A container has disappeared {{ $labels.name }}

