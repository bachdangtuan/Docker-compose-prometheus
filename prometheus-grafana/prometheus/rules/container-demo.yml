groups:
- name: Container
  rules:
  - alert: Container Tắt
    expr: absent(container_start_time_seconds{name=~".*"})
    for: 1s
    labels:
      severity: critical
    annotations:
      summary: Container killed (instance {{ $labels.instance }})
      description: |
       Tuấn
       A container has disappeared

