groups:
- name: Container
  rules:
  - alert: Container Tắt
    expr: time() - container_last_seen > 60
    for: 1m
    labels:
      severity: critical
    annotations:
      Description: |
        {{$labels.name}}
           {{$labels.instance}}
           {{$labels.image}}
        A container is down for 30s