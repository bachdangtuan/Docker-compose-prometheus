global:
  scrape_interval:     5s
  evaluation_interval: 5s 
  external_labels:
      monitor: 'my-project'
rule_files:
  - 'alert.rules'
  # - "first.rules"
  # - "second.rules"

# Alert Manager
alerting:
  alertmanagers:
  - scheme: http
    static_configs:
    - targets: ['10.0.0.232:9093']
    basic_auth:
      username: admin
      password: vietnam@1234

# Scarpe- Config

scrape_configs:
  - job_name: 'server-dev'
    scrape_interval: 5s
    static_configs:
         - targets: ['10.0.0.55:9100']
