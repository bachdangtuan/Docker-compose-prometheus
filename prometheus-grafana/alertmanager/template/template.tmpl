{{ if gt (len .Alerts.Firing) 0 }}
Alerts Firing:
{{ range .Alerts.Firing }}
  {{ range .Labels }}
    {{ if eq .name "alertname" }}
      {{ .name }} = {{ .value }}
    {{ end }}
  {{ end }}
{{ end }}
{{ end }}
